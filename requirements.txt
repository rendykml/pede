# === PDF Processing ===
pymupdf4llm>=0.0.17
PyMuPDF>=1.25.0
paddleocr>=2.9.1
paddlepaddle<3.0.0
pdf2image>=1.17.0

# === Text Splitting ===
langchain-text-splitters>=0.3.8

# === Embedding ===
# FlagEmbedding: BGE-M3 hybrid (dense + sparse/lexical) dalam satu model.
FlagEmbedding>=1.3.0
sentence-transformers>=3.0.0
einops>=0.7.0

# === Vector Database ===
# Butuh dukungan sparse vectors + Query API (Prefetch/Fusion RRF) -> >= 1.10
qdrant-client>=1.12.0

# === HTTP Client (CrossRef API) ===
httpx>=0.28.0

# === API Server & Environment ===
fastapi>=0.115.0
uvicorn>=0.34.0
python-dotenv>=1.0.0
